meta {
  name: Validate (stub - not yet implemented)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/codes/validate?code=ABCD-1234-EFGH
  body: none
  auth: none
}

params:query {
  code: ABCD-1234-EFGH
}

docs {
  ## GET /api/codes/validate?code=...

  **未実装** (D-1 タスク)。

  拡張側 background.js が叩くフレンドコード検証 API。
  実装後の期待レスポンス:

  - 有効: `{ valid: true, expires: "2026-06-01T00:00:00Z" }`
  - 無効: `{ valid: false, reason: "expired" | "not_found" }`

  実装場所: `app/api/codes/validate/route.ts`
}
